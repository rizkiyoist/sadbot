Sadbot is a Telegram Bot that uses OpenAI as the backend so you can chat with it just like in ChatGPT.
Input your config in env.yaml based on env.example.yaml, and initialcond.txt based on initialcond.example.txt to your liking.

To run the bot:
go run .